export interface InputFieldProps {
    placeholder: string;
    type?: string;
    id: string;
}

export interface RegistrationFormData {
    firstName: string;
    lastName: string;
    address: string;
    phone: string;
}